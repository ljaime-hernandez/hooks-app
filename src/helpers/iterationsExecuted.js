
// simple function used for educational purposes
export const IterationsExecuted = (iteration) => {
    for (let i = 0; i< iteration; i++){
         // console.log(i + 1);
    }
    return `${iteration} iterations executed`;
}